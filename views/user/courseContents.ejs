<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= course.title %> Contents</title>
  <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #f8f9fa;
    }
    .container {
      background-color: #fff;
      border-radius: 0.5rem;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      padding: 2rem;
      margin-top: 2rem;
    }
    h2 {
      color: #333;
      text-align: center;
      margin-bottom: 1.5rem;
    }
    .list-group-item {
      display: flex;
      justify-content: space-between;
      align-items: center;
      border: none;
      border-bottom: 1px solid #e9ecef;
    }
    .list-group-item:last-child {
      border-bottom: none;
    }
    .btn-primary {
      background-color: #007bff;
      border-color: #007bff;
    }
    .btn-primary:hover {
      background-color: #0056b3;
      border-color: #0056b3;
    }
    .alert-warning {
      color: #856404;
      background-color: #fff3cd;
      border-color: #ffeeba;
    }
  </style>
</head>
<body>
  <%- include('../partials/navbar', { user: user }) %>
  <div class="container">
    <h2>Contents for <%= course.title %></h2>
    <% if (enrollFirst) { %>
      <div class="alert alert-warning" role="alert">
        You must enroll in this course to access its contents.
      </div>
    <% } %>
    <ul class="list-group">
      <% contents.forEach(content => { %>
        <li class="list-group-item">
          <span><%= content.title %></span>
          <% if (content.type === 'quiz') { %>
            <a href="/courses/<%= course.id %>/quizzes/<%= content.Quiz.id %>" class="btn btn-primary btn-sm">Take Quiz</a>
          <% } else { %>
            <a href="/courses/<%= course.id %>/contents/<%= content.id %>" class="btn btn-primary btn-sm">View</a>
          <% } %>
        </li>
      <% }) %>
    </ul>
  </div>
  <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
